
# --- Core Framework ---
#
# IMPORTANT: The following pins are required for compatibility with FastAPI's HTML template rendering.
# See PR/commit notes for rationale. Do NOT upgrade Starlette or Jinja2 without a full codebase review.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
# Note: Do NOT pin `starlette` here; let FastAPI constrain a compatible Starlette.

# --- Database ---
sqlalchemy>=2.0.35

# --- Validation & Config ---
pymysql>=1.1.0
pydantic>=2.10.0
pydantic-settings>=2.5.0
email-validator>=2.0.0         # required by Pydantic EmailStr
python-dotenv>=1.0.1
pyyaml>=6.0

# --- Authentication (NEW — gap analysis items 2.2 & 2.3) ---
passlib[bcrypt]>=1.7.4     # bcrypt password hashing (replaces SHA-256)
bcrypt==4.0.1              # passlib compatibility (avoids bcrypt 5.x runtime issue)
python-jose[cryptography]>=3.3.0  # JWT token creation & verification

# --- HTTP Client ---
httpx>=0.27.0

# --- Web Scraping ---
firecrawl-py>=1.5.0

# --- LLM Providers ---
openai>=1.50.0
anthropic>=0.39.0

# --- Scheduler ---
apscheduler>=3.10.4

# --- Frontend Templates (NEW — serves the HTML web frontend) ---
jinja2==3.1.3
markupsafe==2.1.3

# --- Testing ---
pytest>=7.4.0
pytest-mock>=3.11.0
