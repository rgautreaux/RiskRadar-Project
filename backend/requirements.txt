# --- Core Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# --- Database ---
sqlalchemy>=2.0.35

# --- Validation & Config ---
pymysql>=1.1.0
pydantic>=2.10.0
pydantic-settings>=2.5.0
email-validator>=2.0.0         # required by Pydantic EmailStr
python-dotenv>=1.0.1
pyyaml>=6.0

# --- Authentication (NEW — gap analysis items 2.2 & 2.3) ---
passlib[bcrypt]>=1.7.4     # bcrypt password hashing (replaces SHA-256)
bcrypt==4.0.1              # passlib compatibility (avoids bcrypt 5.x runtime issue)
python-jose[cryptography]>=3.3.0  # JWT token creation & verification

# --- HTTP Client ---
httpx>=0.27.0

# --- Web Scraping ---
firecrawl-py>=1.5.0

# --- LLM Providers ---
openai>=1.50.0

# --- Scheduler ---
apscheduler>=3.10.4

# --- Frontend Templates (NEW — serves the HTML web frontend) ---
jinja2>=3.1.0

# --- Testing ---
pytest>=7.4.0
pytest-mock>=3.11.0
playwright>=1.35.0
